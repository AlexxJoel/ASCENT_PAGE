---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Contacto - ASCENT">
    <section class="min-h-screen flex py-20 justify-center bg-gradient-to-br from-gray-900 to-black px-4">
        <div class="container max-w-6xl mx-auto">
            <div class="flex flex-col md:flex-row bg-black/40 backdrop-blur-sm rounded-xl shadow-2xl overflow-hidden">
                <!-- Columna izquierda: Información y imagen -->
                <div class="md:w-1/2 flex flex-col justify-center items-center p-8 bg-black/60 text-center">
                    <h2 class="text-2xl md:text-3xl font-bold text-blue-400 mb-4">¡Hablemos sobre tus proyectos!</h2>
                    <p class="text-gray-200 mb-6">
                        Si tienes alguna pregunta, necesitas más información sobre nuestros servicios, o quieres iniciar un proyecto con nosotros, no dudes en escribirnos. Nuestro equipo estará encantado de responder lo antes posible.
                    </p>
                    <img src="/assets/images/ASCENT_WHITE_Large.png" alt="Contacto ASCENT" class="w-40 md:w-56 mb-2 drop-shadow-xl" />
                </div>
                <!-- Columna derecha: Formulario -->
                <div class="md:w-1/2 p-8">
                    <h1 class="text-3xl font-bold text-blue-400 mb-6 text-center animate-fadeIn">Contacto</h1>
                    <form id="contactForm" class="space-y-5">
                        <div>
                            <label for="nombre" class="block text-gray-200 font-semibold">Nombre:</label>
                            <input type="text" id="nombre" name="nombre" required class="mt-1 block w-full rounded-md border border-gray-600 bg-gray-900/70 text-white shadow-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition" />
                        </div>
                        <div>
                            <label for="email" class="block text-gray-200 font-semibold">Email:</label>
                            <input type="email" id="email" name="email" required class="mt-1 block w-full rounded-md border border-gray-600 bg-gray-900/70 text-white shadow-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition" />
                        </div>
                        <div>
                            <label for="mensaje" class="block text-gray-200 font-semibold">Mensaje:</label>
                            <textarea id="mensaje" name="mensaje" required rows="4" class="mt-1 block w-full rounded-md border border-gray-600 bg-gray-900/70 text-white shadow-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition"></textarea>
                        </div>
                        <button type="submit" class="w-full mt-4 bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-full font-bold shadow-lg transition duration-300 animate-fadeIn">Enviar</button>
                    </form>
                    <script type="text/javascript">
                        document.getElementById('contactForm').addEventListener('submit', function(e) {
                            e.preventDefault();
                            function strictEncode(str) {
                                return encodeURIComponent(str).replace(/%40/g, '@');
                            }
                            const nombre = document.getElementById('nombre').value;
                            const email = document.getElementById('email').value;
                            const mensaje = document.getElementById('mensaje').value;
                            const destinatario = strictEncode('ascent@gmail.com'); // Cambia esto por tu correo real
                            const asunto = strictEncode('Nuevo mensaje de contacto de ' + nombre);
                            const cuerpo = strictEncode(`Nombre: ${nombre}\nEmail: ${email}\nMensaje: ${mensaje}`);
                            const gmailUrl = `https://mail.google.com/mail/?view=cm&fs=1&to=${destinatario}&su=${asunto}&body=${cuerpo}`;
                            window.open(gmailUrl, '_blank');
                        });
                    </script>
                </div>
            </div>
        </div>
    </section>
</BaseLayout>